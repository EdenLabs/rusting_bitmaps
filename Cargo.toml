[package]
name    = "rusting-bitmaps"
version = "0.2.0"
authors = ["Techgeek1"]
edition = "2018"

[dev-dependencies]
criterion   = { version = "0.8.2", features = ["html_reports"] }
num-traits  = "0.2.8"
rand        = "0.10.2"

[[bench]]
name    = "queries"
harness = false